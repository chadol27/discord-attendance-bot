# Discord Attendance

빈 `discord.js` + TypeScript 봇 프로젝트.

## Setup

```bash
npm install
```

`DISCORD_TOKEN` 환경 변수를 설정한 뒤 실행해.

```bash
npm run dev
```

## Scripts

- `npm run dev`: TypeScript 소스 직접 실행
- `npm run build`: `dist/`로 빌드
- `npm start`: 빌드된 봇 실행
- `npm run typecheck`: 타입 체크만 실행
